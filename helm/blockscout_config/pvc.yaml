# pvc.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-blockscout-postgresql-0
  namespace: besu
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: blockscout-storage
